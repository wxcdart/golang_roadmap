# Linting and Static Analysis

This folder documents common tools and provides a minimal `golangci-lint` config.

Guidance:
- Use `go vet` for basic static checks: `go vet ./...`
- Use `golangci-lint` for a fast multi-linter: `golangci-lint run`

Example config: `.golangci.yml` is provided.
