# Zerolog examples

This folder contains a minimal example showing how to use `zerolog` for structured logging.

Run:

```bash
cd golang_roadmap/05_logging_beyond_slog/zerolog
go run zerolog_example.go
```
