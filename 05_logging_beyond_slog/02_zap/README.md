# Zap examples

This folder contains a minimal example showing how to use `zap` for structured logging.

Run:

```bash
cd golang_roadmap/05_logging_beyond_slog/zap
go run zap_example.go
```
