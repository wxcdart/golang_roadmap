# Logrus examples

This folder contains a minimal example showing how to use `logrus` for structured logging.

Run:

```bash
cd golang_roadmap/05_logging_beyond_slog/logrus
go run logrus_example.go
```
